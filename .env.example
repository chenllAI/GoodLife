# 部署配置（可选）
#
# 仓库地址是**构建时**烘焙进产物的，这样任何人打开链接就直接进入榜单，
# 不需要自己填仓库信息。
#
# 不设这些变量也能用 —— 默认值是 chenllAI/GoodLife。
# 想把这个项目部署到你自己的仓库时，复制此文件为 .env.local 并改成你的值。
#
# ⚠️ 这里**只能放仓库地址**。
# Token 是凭据，绝不能写进这里 —— 它会被打包进公开的网页，
# 任何人查看源码就能拿到，而且会永久留在 git 历史里。

# 数据仓库的所有者（GitHub 用户名或组织名）
VITE_GH_OWNER=chenllAI

# 数据仓库名
VITE_GH_REPO=GoodLife

# 数据所在分支
VITE_GH_BRANCH=main

# 数据所在目录（仓库内的相对路径）
VITE_GH_BASE_PATH=data
